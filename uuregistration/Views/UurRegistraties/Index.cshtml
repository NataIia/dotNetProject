@model uuregistration.ViewModels.UurRegistratieViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => Model.UurRegistratie.Titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => Model.UurRegistratie.Omschrijving)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.UurRegistraties) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Omschrijving)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UurRegistratieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UurRegistratieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UurRegistratieId })
        </td>
    </tr>
}

</table>
